function somar(){
    let soma = 0

    let cont =1
    while (cont <= 100 && soma <= 400){
        console.log(cont)
        soma = soma + cont
        console.log ("cont: " + cont)
        console.log ("cont: " + cont)
        
        cont++
    }
    console.log ("soma :" + soma)
}

function contar(){
    let cont = 0 
    while (cont < 0 ){
        console.log(cont)

        cont++
    }
}

for